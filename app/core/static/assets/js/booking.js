// booking.js intentionally left blank  if (document.getElementById("bookingSentFlag")) {    // Sidebar may still be open after redirect if history cached; close it    const closeBtn = document.querySelector(".close-side-widget");    if (closeBtn) closeBtn.click();    showSuccessModal();  }}); {  const modal = document.getElementById("bookingSuccessModal");  if (!modal) return;  modal.style.display = "flex";  modal.classList.add("show");  setTimeout(() => {    modal.classList.add("fadeOut");    setTimeout(() => {      modal.classList.remove("show", "fadeOut");      modal.style.display = "none";    }, 700);  }, 2500);}  if (document.getElementById("bookingSentFlag")) {      // close sidebar if still open      const closeBtn = document.querySelector(".close-side-widget");      if (closeBtn) closeBtn.click();      showSuccessModal();  } = new FormData(bookingForm);        try {            const response = await fetch(bookingForm.action, {                method: "POST",                headers: { "X-Requested-With": "XMLHttpRequest" },                body: formData,            });            if (!response.ok) {                console.error("Booking request failed", await response.text());                return;            }            // Close sidebar robustly            const closeBtn = document.querySelector(".close-side-widget");            if (closeBtn) closeBtn.click();            else {                const sidebar = document.querySelector(".xs-sidebar-group");                sidebar?.classList.remove("isActive");                document.body.classList.remove("locked");            }            // Reset form fields            bookingForm.reset();            // Show success modal for 2.5s            showSuccessModal();        } catch (err) {            console.error(err);        }    });    if (!modal) return;    modal.style.display = "flex";    modal.classList.add("show");    setTimeout(() => {        modal.classList.add("fadeOut");        setTimeout(() => {            modal.classList.remove("show", "fadeOut");            modal.style.display = "none";        }, 700);    }, 2500);}
